# Local server defaults
PORT=3001
ROOMLAB_DB=./data/roomlab.sqlite
TRUST_PROXY=0
# Set only when the deployment has one canonical public URL.
# APP_ORIGIN=https://rooms.example.com
# Optional visual QA only; never required by the runtime.
OPENROUTER_API_KEY=
